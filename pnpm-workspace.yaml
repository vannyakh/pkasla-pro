# PNPM Workspace Configuration
# This file defines the workspace structure for the monorepo

packages:
  # Applications - Frontend and Backend
  - "apps/web"
  - "apps/backend"
  
  # Shared Packages - Reusable code, configs, and utilities
  - "packages/*"
